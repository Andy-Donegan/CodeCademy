// JavaScript source code

class Surgeon {
    constructor(name, department) {
        this.name = name;
        this.department = department;
    }
}

// create new Surgeon object and log output to console for testing.
const timmySurgeon = new Surgeon("Timmy", "Urinals");

console.log(timmySurgeon);